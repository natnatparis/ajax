<h1><%= @email.object %></h1>
<p><%= @email.body %></p><br><br>

<%= link_to "Delete", email_path(@email), method: :delete, remote: true, class:"btn btn-info" %>
<button class="bg-success">You haven't read this one  yet</button>
